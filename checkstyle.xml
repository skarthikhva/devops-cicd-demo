<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Lint stage for the CI pipeline. Deliberately practical, not exhaustive: catches the
  kind of thing that actually causes bugs or review friction (unused imports, dead code,
  suspicious equality/switch usage), without demanding a Javadoc rewrite of the whole
  codebase the way google_checks.xml or sun_checks.xml would out of the box.
-->
<module name="Checker">
    <property name="severity" value="warning"/>

    <module name="TreeWalker">
        <!-- Imports -->
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <module name="AvoidStarImport"/>

        <!-- Common bug patterns -->
        <module name="EqualsHashCode"/>
        <module name="EmptyBlock">
            <property name="option" value="text"/>
        </module>
        <module name="MissingSwitchDefault"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>
        <module name="StringLiteralEquality"/>

        <!-- Whitespace & formatting -->
        <module name="WhitespaceAfter"/>
        <module name="NoWhitespaceAfter">
            <property name="tokens" value="INC,DEC,UNARY_MINUS,UNARY_PLUS,BNOT,LNOT,DOT,ARRAY_DECLARATOR,INDEX_OP"/>
        </module>

        <!-- Naming conventions -->
        <module name="MethodName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
    </module>

    <!-- File-level -->
    <module name="FileTabCharacter"/>
</module>
